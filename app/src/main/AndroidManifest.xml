<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aiyiqi.bwf.com.aiyiqi_lf">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".util.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gui.activity.WellComeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".gui.activity.RenderingActivity" />
        <activity android:name=".gui.activity.BudgetActivity" />
        <activity android:name=".gui.activity.CityActivity" />
        <activity android:name=".gui.activity.CompanyActivity" />
        <activity android:name=".gui.activity.HomeActivity" />
        <activity android:name=".gui.activity.MaterialsActivity" />
        <activity android:name=".gui.activity.SchoolActivity" />
        <activity android:name=".gui.activity.SelfHelpActivity" />
        <activity android:name=".gui.activity.DesignActivity" />

    </application>

</manifest>